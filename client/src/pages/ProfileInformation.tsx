import { Text } from '@chakra-ui/react';
import Layout from '../components/dashboard/layout/Layout';
const ProfileInformation = () => {
  return (
    <Layout>
      <Text>Add a new job in here</Text>
    </Layout>
  );
};

export default ProfileInformation;
