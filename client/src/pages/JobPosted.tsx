import { Text } from '@chakra-ui/react';
import Layout from '../components/dashboard/layout/Layout';
const JobPosted = () => {
  return (
    <Layout>
      <Text>Find all the jobs that are posted by you!</Text>
    </Layout>
  );
};

export default JobPosted;
