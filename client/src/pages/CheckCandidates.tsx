import { Text } from '@chakra-ui/react';
import Layout from '../components/dashboard/layout/Layout';
const CheckCandidates = () => {
  return (
    <Layout>
      <Text>Check all candidates</Text>
    </Layout>
  );
};

export default CheckCandidates;
